/* v5 dashboard — content only; the shell lives in dashboard-v5/layout.tsx. */

import { V5Dashboard } from "@/components/v5/dashboard";

export default function DashboardV5Page() {
  return <V5Dashboard />;
}
