/* v5 dashboard — content only; the shell lives in dashboard-v6/layout.tsx. */

import { V6Dashboard } from "@/components/v6/dashboard";

export default function DashboardV5Page() {
  return <V6Dashboard />;
}
